@model IEnumerable<Travel_agency.Models.TravelGuide>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TravelGuideName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TravelGuideDomain)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Client)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TravelGuideName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TravelGuideDomain)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Client.ClientId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TravelGuideId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TravelGuideId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TravelGuideId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
